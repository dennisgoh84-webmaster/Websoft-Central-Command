<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * Central Command's seeding is `php artisan cc:install` (migrate +
 * ensure the default admin exists) — see
 * app/Console/Commands/CentralCommandInstall.php. This class exists
 * only because Laravel's framework expects one; it is intentionally
 * unused.
 */
class DatabaseSeeder extends Seeder
{
    public function run(): void {}
}
